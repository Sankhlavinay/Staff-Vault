<!DOCTYPE html>
<html>
  <head>
    <title>StaffVault</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%= render 'shared/header', q: Employee.ransack(params[:q]) %>
    <p class="notice text-center mt-2"><%= notice %></p>
    <p class="alert text-center mt-2"><%= alert %></p>
    
    <div class="container">
      <div class="row">
        <%= yield %>
      </div>
    </div>
    <% if !(params[:controller] == 'employees') %>
      <%= render 'shared/footer' %>
    <% end %>
  </body>
</html>